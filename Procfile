# Persistent feeder (Railway/Heroku-style). Set PRIVATE_KEY, FHE_ORACLE_BRIDGE, RPC_* in the platform env.
# Railway: create a worker service or set this as the start command; no HTTP port required.
worker: npx hardhat run scripts/feederDaemon.js --network arbitrumSepolia
